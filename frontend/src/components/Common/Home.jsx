// import { useState } from "react";

function Home() {
	
	// If user is not logged in, stay on screen and render login screen
	// if the user is logged in, direct to cart with loading screen
	// const [renderLogin, setRenderLogin] = useState(true);
	
	return (
		<div>
			HomePage Here!
		</div>
	)
}

export default Home;